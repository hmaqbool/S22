#Arithmetic formatter

def arithmetic_arranger(problems,y=True): #x is array of strings
    answer=''
    if len(x) > 5:
        answer="Error: Too many problems."
        return answer
    operations= list(map(lambda x: x.split()[1],problems))
    
    
    
    
    



def main():
    temp= str(input("Enter the)
